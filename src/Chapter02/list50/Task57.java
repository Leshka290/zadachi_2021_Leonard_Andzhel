package Chapter02.list50;

/*
Блоки инструкций. Написать фрагмент кода с примером выражения switch
JDK 12, в котором метки вариантов case указывают на блок в фигурных скобках.
 */
public class Task57 {

    /*
    private static Player createPlayer(PlayerTypes playerType) {
        return switch (playerType) {
            case TENNIS, GOLF -> {
                System.out.println("Individual game");
                break new Individual();
            }
            case FOOTBALL, VOLLEYBALL -> {
                System.out.println("Team game");
                break new Team();
            }
            default -> throw new IllegalArgumentException("Недопустимый тип игрока: " + playerType);
        };
    }
     */

}
