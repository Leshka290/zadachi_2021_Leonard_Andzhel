package Chapter04.list90;

/*
LVTI и возвращаемый и параметрический типы метода. Написать
несколько фрагментов кода, которые иллюстрируют использование LVTI и
методов Java в терминах возвращаемых и параметрических типов.
 */
public class Task92 {

    /*
    ♦ LVTI нельзя использовать в качестве возвращаемого типа из метода, следующий
ниже код не компилируется:
    //Не компилируется

    public var fetchReport(Player player, Date timestamp) {

    return new Report();
    }

    LVTI нельзя использовать в качестве аргументного типа внутри объявления
метода, следующий ниже код не компилируется:

    public Report fetchReport(var player, var timestamp) {

    return new Report();
    }
     */
}
